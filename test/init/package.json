{
  "name": "kyoko-init",
  "version": "0.0.1",
  "description": "Get started with Playwright and Cucumber.",
  "scripts": {
    "pretest": "playwright install --with-deps chromium",
    "test": "cucumber-js -c cucumber.js",
    "report": "allure generate --single-file results/allure/ -c -o results/allure/html/"
  },
  "dependencies": {
    "@iamkenos/kyoko": "*"
  }
}
